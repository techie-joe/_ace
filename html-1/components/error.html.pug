extends _error

block append vars
  - filename = 'error.html'
  - heading = 'Error page'
  - about   = 'Ready made templates when you are experiencing dificulties.'

block append style
  style.
    #_body:before { content:'#{layout}/err' }