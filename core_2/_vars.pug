//- ==============================================================
//- Core variables
//- ==============================================================
-
  //- project
  let name           = '[project_name]';
  let version        = '[version]';
  let build          = '[build]';

  //- credit
  let author         = '[unknown]';
  let publisher      = '[unknown]';

  //- site
  let title          = '[untitled]';
  let description    = '[undescribed]';

  let heading        = '[untitled]';
  let about          = '[undescribed]';

  //- layout
  let layout         = '[undefined]';

  //- urls
  let filename       = '[undefined]';
  let siteUrl        = '[site_url]';
  let authorUrl      = '[author_url]';
  let sourceUrl      = '[source_url]';
  let builderUrl     = '[builder_url]';
  let localUrl       = '[local_url]';

  //- open graph metadata for social media sharing
  let og_url         = siteUrl + '?utm_source=homescreen';
  let og_type        = 'website';
  let og_title       = title;
  let og_description = description;
  let og_image       = '';
  let og_imageAlt    = '';

  //- uris
  let homeUri        = '/ace/';
  let assetsUri      = '/ace/assets/';
  let elementsUri    = '/ace/elements/';

  //- metadata
  let now            = new Date();
  let thisYear       = now.getFullYear();
  let copyright      = `Copyright ${thisYear} - ${author}`;

  //- en or en-US following the BCP 47 language tag standard
  let lang           = 'en';
  let charset        = 'utf-8';

  //- styling
  let colorScheme    = 'light dark';
  let themeColor     = '#222222';
  let htmlClass      = [];
  let bodyClass      = [];

  //- progressive web app metadata
  let INCLUDE_MANIFEST        = false;
  let INCLUDE_THEMEJS         = true;
  let INCLUDE_SERVICE_WORKERS = false;

  //- project status
  const LOCALHOST      = 'LOCALHOST';
  const DEVELOPMENT    = 'DEVELOPMENT';
  const PRODUCTION     = 'PRODUCTION';
  let   PROJECT_STATUS = DEVELOPMENT;

//- ==============================================================
//- Process vars
//- ==============================================================
-
  if(INCLUDE_THEMEJS) { htmlClass.push('_hidden') }

//- ==============================================================
//- Update functions
//- ==============================================================
-
  function updateTitle( _title ){
    title    = _title
    og_title = _title
  }

  function updateDescription( _description ){
    description    = _description
    og_description = _description
  }

  function updateUrl( _url='', _type='website' ){
    siteUrl = _url
    og_url  = _url? _url + '?utm_source=homescreen' : '';
    og_type = _type
  }

  function updateImage( _uri, _alt ){
    og_image    = _uri
    og_imageAlt = _alt
  }

//- ==============================================================
//- Variable mixins
//- ==============================================================

mixin pageData
  ._scrollable._full_width._text_left&attributes(attributes): +pre: +putData

mixin putData
  +l
  +join([
    `title            = ${title}`,
    `description      = ${description}`,
    ``,
    `heading          = ${heading}`,
    `about            = ${about}`,
    ``,
    `layout           = ${layout}`,
    `filename         = ${filename}`,
    `siteUrl          = ${siteUrl}`,
    ``,
    `og_url           = ${og_url}`,
    `og_type          = ${og_type}`,
    `og_title         = ${og_title}`,
    `og_description   = ${og_description}`,
    `og_image         = ${og_image}`,
    `og_imageAlt      = ${og_imageAlt}`,
    ``,
    `homeUri          = ${homeUri}`,
    `assetsUri        = ${assetsUri}`,
    ``,
    `name             = ${name}`,
    `version          = ${version}`,
    `build            = ${build}`,
    ``,
    `author           = ${author}`,
    `publisher        = ${publisher}`,
  ])
  +l

//- ==============================================================
//- Core functions
//- ==============================================================

include _functions
include _mixins
include _snips

//- ==============================================================
//- Include project specific configurations
//- ==============================================================

include _CONFIGS

//- ==============================================================
