extends _html

block append vars
  - layout  = 'core/error'
  - htmlClass = addClass(htmlClass,'_errorpage')

block seo
  title !{title||'[untitled]'}
  meta(name="description" content!=description||'[undescribed]')/

block append style
  +cssv(assetsUri+'css/errorpage.css')