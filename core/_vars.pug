//- ==============================================================
//- Core functions
//- ==============================================================

include _functions
include _mixins

//- ==============================================================
//- Core variables
//- ==============================================================
-
  //- temporary variable for small operations
  let temp = undefined;

  //- project
  let name           = '[project_name]';
  let version        = '[version]';
  let build          = '[build]';

  //- credit
  let author         = '[unknown]';
  let publisher      = '[unknown]';

  //- site
  let title          = '[untitled]';
  let description    = '[undescribed]';

  let heading        = '[untitled]';
  let about          = '[undescribed]';

  //- urls
  let siteUrl        = '[site_url]';
  let authorUrl      = '[author_url]';
  let sourceUrl      = '[source_url]';
  let builderUrl     = '[builder_url]';
  let localUrl       = '[local_url]';

  //- uris
  let homeUri        = '/ace/';
  let assetsUri      = '/ace/assets/';
  let coreUri        = '/ace/core/';

  //- open graph metadata for social media sharing
  let og_url         = siteUrl + '?utm_source=homescreen';
  let og_title       = title;
  let og_description = description;
  let og_type        = 'website';
  let og_image       = '';
  let og_imageAlt    = '';

  //- layout
  let layout         = '[n/a]';

  //- metadata
  let now            = new Date();
  let thisYear       = now.getFullYear();
  let copyright      = `Copyright ${thisYear} - ${author}`;

  //- en or en-US following the BCP 47 language tag standard
  let lang           = 'en';
  let charset        = 'utf-8';

  //- styling
  let colorScheme    = 'light dark';
  let themeColor     = '#222222';
  let htmlClass      = '';
  let bodyClass      = '';
  let contentsClass  = '';

  //- progressive web app metadata
  let INCLUDE_MANIFEST = false;
  let INCLUDE_SERVICE_WORKERS = false;

  //- project
  const LOCALHOST      = 'localhost';
  const DEVELOPMENT    = 'DEVELOPMENT';
  const PRODUCTION     = 'PRODUCTION';
  const PROJECT_STATUS = DEVELOPMENT;

//- --------------------------------------------------------------
-

  let js   = {}; // javascripts
  let cssi = {}; // CSS images
  let svgi = {}; // SVG images

  js = {
    themeChange : 'theme.change();',
    goBack      : 'window.history.back();',
    print       : 'window.print();',
  };

  svgi.compare = 'M19,3H14V5H19V18L14,12V21H19A2,2 0 0,0 21,19V5C21,3.89 20.1,3 19,3M10,18H5L10,12M10,3H5C3.89,3 3,3.89 3,5V19A2,2 0 0,0 5,21H10V23H12V1H10V3Z'; // for theme switch

//- ==============================================================
//- Include project specific configurations
//- ==============================================================

include _CONFIGS

//- ==============================================================
//- Update functions
//- ==============================================================
-
  function updateTitle( _title ){
    title    = _title
    og_title = _title
  }

  function updateDescription( _description ){
    description    = _description
    og_description = _description
  }

  function updateUrl( _url='', _type='website' ){
    siteUrl = _url
    og_url  = _url? _url + '?utm_source=homescreen' : '';
    og_type = _type
  }

  function updateImage( _uri, _alt ){
    og_image    = _uri
    og_imageAlt = _alt
  }

//- ==============================================================
//- Data mixins
//- ==============================================================

mixin putData
  +l
  +join([
    `name             = ${name}`,
    `version          = ${version}`,
    `build            = ${build}`,
    ``,
    `author           = ${author}`,
    `publisher        = ${publisher}`,
    ``,
    `title            = ${title}`,
    `description      = ${description}`,
    ``,
    `heading          = ${heading}`,
    `about            = ${about}`,
    ``,
    `siteUrl          = ${siteUrl}`,
    `og_url           = ${og_url}`,
    `og_title         = ${og_title}`,
    `og_description   = ${og_description}`,
    `og_type          = ${og_type}`,
    `og_image         = ${og_image}`,
    `og_imageAlt      = ${og_imageAlt}`,
    ``,
    `homeUri          = ${homeUri}`,
    `assetsUri        = ${assetsUri}`,
    ``,
    `layout           = ${layout}`,
  ])
  +l