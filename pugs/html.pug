include _vars
include _jss
include _mixins

block vars
  //- variables for every html page

block entry
  //- entrypoint for variable setup & security check

doctype html
html#_html(lang=lang class=htmlClass)._nojs._scrollbar._a

  | <!-- layout:html/#{codeName} -->
 
  head
    meta(charset=charset)/
    
    block seo
      include _seo

    block meta
      include _meta

    block style
      +htmlColor
      //- +css(href,media='all')
      +css(assetsUrl+'css/style.css?v='+version)

  body#_body(class=bodyClass)

    block contents
      #_contents(class=contentsClass)

        block views
          +view

            block header
              //- +header#_header: +heading#_heading

            block nav
              //- include _nav
              //- +nav#_nav: ul: +la

            block pre
              //- +pre#_pre.

            block main
              //- +main#_main: article| main article
              +main#_main: article| main article

            block footer
              //- +footer#_footer
            
            block asides
              //- +aside#_aside

    block widgets
      //- +widgets#_widgets: +widget#_widget

    block script
      //- +js(src)
      +js(assetsUrl+'cjs/thm.js?v='+version)(defer)

    block last
      | <!-- IE needs 512+ bytes: https://techie-joe.github.io/library/html5/ie#ie-needs-512-bytes -->