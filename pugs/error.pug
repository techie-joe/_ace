extends html

block append vars
  - codeName  = 'error'
  - htmlClass = htmlClass+' _errorpage'

//- block append vars
  +updateTitle( `(untitled)` )
  +updateDescription( `(undescribed)` )

block seo
  title !{title||'(untitled)'}
  meta(name="description" content=description||'(undescribed)')/

block append style
  +css(assetsUrl+'css/errorpage.css?v='+version)