extends _html

block append vars
  - filename = 'index.html'
  - heading  = 'Core Elements'
  - about    = 'Lets explore the building blocks of our templates.'

  - navbar   = true
  - pageLayout = 'flex-to-max'
  - navbarLayout = 'flex-to-max'

block topRightNav_content
  +topThemeSwitch
  +topHomeButton

block pageHeader
  +simpleHeader

block navmenu
  //- nothing

block append style
  //- style.
  //- style.

block article
  article._article

    +nav._color_1._radius_x5r._clearfix._mv
      -
        temp = [
          ['basics.html','Basics'],
          ['texts.html','Texts'],
          ['icons.html','Icons'],
          ['links.html','Links'],
          ['buttons.html','Buttons'],
          ['containers.html','Containers'],
          ['alignment.html','Alignment'],
          ['flex.html','Flex'],
        ];
      each v in temp
        +a._link._l(href=v[0]) #{v[1]}

    +nav._color_1._radius_x5r._clearfix._mv
      -
        temp = [
          ['view.html','View'],
          ['layouts.html','Layouts'],
          ['overflow.html','Overflow'],
          ['menu.html','Menu'],
        ];
      each v in temp
        +a._link._l(href=v[0]) #{v[1]}

    +nav._color_1._radius_x5r._clearfix._mv
      -
        temp = [
          ['html.html','Html'],
          ['blank.html','Blank'],
          ['starter.html','Starter'],
        ];
      each v in temp
        +a._link._l(href=v[0]) #{v[1]}

    +nav._color_1._radius_x5r._clearfix._mv
      -
        temp = [
          ['error.html','Error'],
          ['404','--','Simulate real 404'],
          ['e404.html','404'],
          ['e500.html','500'],
        ];
      each v in temp
        +a._link._l(href=v[0] title=v[2]) #{v[1]}

    +nav._color_1._radius_x5r._clearfix._mv
      -
        temp = [
          ['test.html','Test'],
          ['collapsibles.html','Collapsibles'],
        ];
      each v in temp
        +a._link._l(href=v[0]) #{v[1]}

    +nav._color_1._radius_x5r._clearfix._mv
      -
        temp = [
          [homeUri+'themejs/','ThemeJs'],
          ['theme.html','Theme'],
          ['colors.html','Colors'],
          ['color_tables.html','Color-Tables'],
        ];
      each v in temp
        +a._link._l(href=v[0]) #{v[1]}

    //- hr

    //- +pageData

  //- article

//- block article