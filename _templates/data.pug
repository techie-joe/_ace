include _vars
include _mixins

block vars
  -
  //- layout vars

block entry
  //- entrypoint for variable setup & security check

doctype html
html(lang=lang)

  | <!-- #{shortName}:layout -->

  head
    meta(charset=charset)/

    block seo
      title !{title||'(untitled)'}
      meta(name="description" content=description||'(undescribed)')/

    block style
      +css(assetsUrl+'css/basic.css?v='+version)

  body#_body(class=bodyClass)

    block content
      #_contents(class=contentClass)

        block view
          +view

            block header
              //- +header#_header

            block nav
              //- +nav#_nav: ul: +la
              //- include _nav

            block pre
              //- +pre#_pre: pre._pre

            block main
              //- +main#_main: article| main article
              +main#_main: article| main article

            block footer
              //- +footer#_footer
            
            block aside
              //- +aside#_aside

    block widgets
      //- +widgets#_widgets: +widget#_widget

    block last
      | <!-- IE needs 512+ bytes: https://techie-joe.github.io/library/html5/ie#ie-needs-512-bytes -->