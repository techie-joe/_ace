extends ../pugs/html

block append vars
  include _vars

  - heading = `Blank Page`
  - about   = `Just a blank page to start building.`
  - updateTitle( `${heading} | ${title}` )
  - updateDescription( `${about}` )
  - updateUrl( url+"blank.html" )

//- block main
  //- +main#_main: article| main article
  +pageData#_pageData._page_layout
