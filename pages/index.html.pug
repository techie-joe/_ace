extends _html

block append vars
  - heading = `Ace Template`
  - about   = `A solid foundation to build your website and application. By using our simple approach, you'll get full control from the very beginning.`
  - updateTitle( heading )

block append style
  +cssv(assetsUrl+'css/demo.css')
  style.
    #_header ._header_layout { padding: 2rem 3rem; }
    @media (max-width: 530px) {
    #_header ._header_layout { padding: 3rem 1rem 2rem; }
    }

    #_header ._header_layout { position:relative }
    #_header ._header_layout:before {
      content: 'A';
      font-family: serif;
      font-weight: bold;
      font-size: 40rem;
      display: block;
      line-height: 0.7;
      position: absolute;
      top: -55%;
      right: 4%;
      opacity: 0.1;
      user-select: none;
    }
    #_main,
    #intro { position:relative; z-index:1 }

    #intro { max-width: 30rem; }

    #intro ._hero-1 span { line-height:1 }

    #topi { position: absolute; top: 3.3rem; right: 5rem; }
    #topi ._isvg { height: 2.5em; min-width: 2.5em; }
    @media (max-width: 530px) {
    #topi { top: 1rem; right: 1rem; }
    #topi ._isvg { height: 1.5em; min-width: 1.5em; }
    }
    #topi ._nav-item { padding:.5em }

    html   #_header { background: #ccc; }
    ._dark #_header { background: #111; }

    html   #_header h1 { color: #000; }
    ._dark #_header h1 { color: #fff; }

    #_html .pages ul { list-style-type: none; padding-inline-start: 0; }

block header
  +header#_header
    #intro
      h1._hero-1 #[span.t1 Ace] #[span.t2._tc_primary Template]
      p._smaller by #[+a(authorUrl)._stay-fresh !{author}]
      p._text-justify !{about}
    #topi
      +changeThemeLink._nav-item._nav-icon._hover-scale: +isvg(svgi.compare)

block main
  +main#_main: article

    include demo/_nav

    section._mv-2

      p._text-justify This web template is equipped with #[b._h7 pre-made building components] and #[b._h7 helper classes] to give you an easy building experience. We have simplified web creation to make it accessible to everyone, even for those who may not have technical expertise. Dive in and start building your website straight away.

      ._mv._nav-color._radius-x5r: +nav#_nav
        b._nav-label| Template Components : &nbsp;
        +menu

      hr

    section._mv-2
      h2 Examples

      mixin lia_example_1(ref='')
        +lia("/ace/example_1/"+ref)._nav-item: block

      nav.pages: ._flex
        ._f-12-2
          ul._no-margin
            +lia_example_1()| Example #1 - Simple Home

    section._mv-2
      h2 Basic Templates

      - let templateUrl    = "/ace/templates/";
      mixin lia_template(ref='')
        +lia(templateUrl+ref)._nav-item: block

      nav.pages
        ._flex
          ._f-12-4
            ul._no-margin
              +lia_template('full_screen.html')| Full Screen
              +lia_template('split_screen.html')| Split Screen
              +lia_template('flex_gallery.html')| Flex Gallery
          ._f-12-4
            ul._no-margin
              +lia_template('classic_page.html')| Classic Page
              +lia_template('classic_list.html')| Classic List
              +lia_template("blank.html")| Blank Page
          ._f-12-4
            ul._no-margin
              li
                +a("404.html","When browser lands on a missing page.")._nav-item| 404 -- Not found
                +_a("---","Simulate Real 404")._nav-item._nav-icon: +middots
              +lia("500.html","When server encounters an error.")._nav-item| 500 -- Oops!
              +lia("closed.html","When you decide to temporarily disable your website.")._nav-item| Temporary closed
        //- ._flex
          ._f-12-4
            ul._no-margin
              +lia('blank.html')._nav-item| Bare Blank Page

    section._mv-2
      h2 Available in light and dark

      p._text-justify Our template is readily available on both light and dark preference. User can easily switch between the two by using the keyboard shortcut #[code._code._no-wrap='Ctrl + Alt + T'] or you can programatically execute #[code._code._no-wrap=js.themeChange] with a button like this one:

      p: +changeThemeLink._btnlink._small #[+isvg(svgi.compare)] #[i._btnpad] #[span._btnlabel Change Theme ] #[i._btnpad]

    hr

    p._smaller
      | Version : v#{version} -- b#{build}

    p._smaller
      - source_url = 'https://github.com/techie-joe/ace'
      | Live __ @ #[+a(url)(target="ace-at-github") #{url}]#[br]
      | Source @ #[+a(source_url)(target="ace-source") #{source_url}]

    p._smaller| Some images on this website are courtesy of Matt at <a href="https://www.svgbackgrounds.com/set/free-svg-backgrounds-and-patterns/">svgbackgrounds.com</a>. Thank you for providing them for free.

block footer
  +footer#_footer
    hr
    p !{copyright}
