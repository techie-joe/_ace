extends _html

block append vars
  - heading = `Core`
  - about   = `The structure of ${name}.`
  - updateTitle( `${heading} | ${name}` )
  - updateDescription( `${about}` )
  - updateUrl( siteUrl+'index.html' )

block append style
  //- style.
  //- style.

//- block main

  +main#_main: article
    +pageData#_pageData
  //- main

//- block main