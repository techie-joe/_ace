extends ../_templates/demo

block append vars
  include _demo-vars
  -
    header      = `blank_demo`
    url         = url+`/blank_demo.html`
    title       = `${header} | ${name}`
    description = `${header} - (undescribed)`

    og_title       = title
    og_description = description
    og_url         = url
  //- 

block append style
  style.
    ._layout { margin:1em }
  //- 

block main
  main#_main: article|This is #{header}