extends _html

block append vars
  - heading = `Simple Home`
  - updateTitle( `${heading} | ${title}` )
  - updateDescription( `A little example of a new website.` )
  - updateUrl( url+"index.html" )

block append style
  //- style.

block main
  +main#_main: article
    p Write your interesting content here. #[+a(homeUrl)._btnlink._smaller Ok]
  
