extends _html

block append vars
  - heading = `Texts`
  - updateTitle( `${heading} | ${title}` )
  - updateDescription( `Font sizes and spacings has been meticulously adjusted to pixel precision for optimal adaptability.` )
  - updateUrl( url+"texts.html" )
  - cssi.gridlines = 'data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="1" height="12"><rect width="1" height="1" fill="%23A9790688" /></svg>';

block append style
  +cssv(assetsUrl+'pure/grid.css')
  style.
    .gridlines {
      min-height: 100px;
      padding-bottom: 3px;
      background-image: url('!{cssi.gridlines}');
      background-repeat: repeat;
    }
    .demo .u { margin: 0 2px; padding-bottom: 10px; }

    .io1  { background: #66668844; box-shadow:0 1px orange; }
    .io2  { background: #66886644; box-shadow:0 1px orange; }

  //- style.

block main
  +main#_main: article

    section.g.demo.gridlines

      mixin rag(x)
        - var n = 0
        while n < x
          if(n%2==0)
            .io2 H#[sup._sup H]g#[sub._sub #{n+1}]
          else
            .io1 &nbsp;
          - n++

      .u._text_1: +rag(13)
      .u._text_2: +rag(12)
      .u._text_3: +rag(11)
      .u._text_4: +rag(10)
      .u._text_5: +rag(9)
      .u._text_6: +rag(8)
      .u._text_7: +rag(7)

      .u._hero:   +rag(9)
      .u._hero_1: +rag(8)
      .u._hero_2: +rag(7)

    section.gridlinesx._text_center
      hr

      - var heros     = 'Text Sample';
      - var headings  = 'Text Sample';
      p._hero_2  #{heros} 3
      p._hero_1  #{heros} 2
      p._hero    #{heros} 1
      h1._h1  #{headings} 1
      h2._h2  #{headings} 2
      h3._h3  #{headings} 3
      h4._h4  #{headings} 4
      h5._h5  #{headings} 5
      h6._h6  #{headings} 6
      div._h7 #{headings} 7
      p Normal Text
      p
        span Normal
        +_
        span._small Small
        +_
        span._smaller Smaller
      p Sizing classes from #[code._code _h1] to #[code._code _h7] separates visual appearance from semantics, giving you better control over both content and design.

    section
      hr

      ._flex._gap_2
        ._f-1
          h2._sans._h2 System Font
          p._system._text_justify.
            System font is the default set of fonts used by an operating system for displaying text across the user interface. The primary goal is to provide a seamless experience for users as they interact with different applications and system elements. Common examples includes "Segoe UI" on Windows, "San Francisco" on macOS, and "Roboto" on Android. They are an integral part of the overall design language and contribute to the visual identity of the operating system.

        ._f_3_1
          h2._sans._h2 Sans
          p._sans._text_justify.
            Sans fonts, such as Arial and Helvetica, are known for their clean and modern look, lacking the small decorative strokes at the ends of letters. They are widely used for digital media due to their readability on screens.

        ._f_3_1
          h2._serif._h2 Serif
          p._serif._text_justify.
            Serif fonts, like Times New Roman, Garamond and Georgia, feature small lines or strokes attached to the ends of letters, giving a more traditional and formal appearance. These fonts are often used in print media for their elegant look.

        ._f_3_1
          h2._mono._h2 Monospace
          p._mono._text_justify.
            Monospace fonts, such as Courier New and Consolas, have characters that occupy the same horizontal space. This uniform spacing makes them ideal for coding, as they enhance readability.

      //- flex


