extends _template

block append vars
  include _vars
  -
    header      = `Blank`
    url         = url+`blank.html`
    title       = `${header} | ${name}`
    description = `This is about ${header}.`

    og_title       = title
    og_description = description
    og_url         = url
  //- 

block append style
  //- style.
  //- 

block header

block main
  +main#_main: article._m| #{description}

block footer
